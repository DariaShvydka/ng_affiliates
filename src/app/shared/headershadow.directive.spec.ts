import { HeadershadowDirective } from './headershadow.directive';

describe('HeadershadowDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadershadowDirective();
    expect(directive).toBeTruthy();
  });
});
